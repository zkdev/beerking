{
   "type": "postgres",
   "host": "localhost",
   "port": 5432,
   "username": "postgres",
   "password": "zkdev#",
   "database": "postgres",
   "synchronize": true,
   "logging": false,
   "entities": [
      "**/*.entity{ .ts,.js}"
   ],
   "migrations": [
      "src/migration/**/*.ts"
   ],
   "subscribers": [
      "src/subscriber/**/*.ts"
   ]
}
